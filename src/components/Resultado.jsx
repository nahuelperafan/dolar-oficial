import styled from "@emotion/styled"

const Resultado = ({resultado}) => {
    const {fecha,compra,venta} = resultado
    console.log(resultado)
    
  return (
    <h1></h1>
  )
}

export default Resultado